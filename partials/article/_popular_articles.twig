{% set popularArticles = _Article.getPopularArticles({ limit: 3, excludeArticleIds : articleId }) %}
<div class="article__related-title_container {% if popularArticles|length <= 0 %} hide {% endif %}">
    <div class="text-center article__related-title">Articles you may be interested in</div>
</div>
{% if popularArticles|length > 0 %}
<div class="article__related-container">
    <div class="card-container">
        <div class="card-row">
            {% set i = 1 %}
            {% for article in popularArticles %}
                {{this.render('../_single-article.twig', {article: article, 'position': i, containerClass: 'col-third'}) | raw}}
                {% set i = i+1 %}
            {% endfor %}
        </div>
    </div>
</div>
{% endif %}